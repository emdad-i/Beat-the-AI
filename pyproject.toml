[project]
name = "beat-the-ai"
version = "0.1.0"
description = "Interactive AI debate game"
requires-python = ">=3.12,<3.13"
dependencies = [
    "flask",
    "flask-socketio",
    "openai",
    "gevent",
    "gevent-websocket",
    "gunicorn",
]

[tool.uv]
package = false