name: Deploy to Linux Server

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      # STEP 1: Upload code using SCP
      - name: Copy files via SSH
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ vars.SERVER_IP }}
          username: ${{ vars.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ vars.SSH_PORT }} # Will use 50011
          source: "."
          target: "/home/${{ vars.SERVER_USER }}/beat-the-ai"

      # STEP 2: Build and Run using Podman
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ vars.SERVER_IP }}
          username: ${{ vars.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ vars.SSH_PORT }}
          script: |
            # Navigate to the home directory folder
            cd /home/${{ vars.SERVER_USER }}/beat-the-ai
            
            # Stop and cleanup existing container
            podman stop beat-the-ai || true
            podman rm beat-the-ai || true

            # Build the image
            podman build -t beat-the-ai .

            # Run the container
            # App listens on 5001 to avoid AirPlay conflicts
            podman run -d \
              --name beat-the-ai \
              -p 5001:5001 \
              --restart always \
              -e OPENAI_API_KEY="${{ secrets.OPENAI_API_KEY }}" \
              beat-the-ai